package com.raffastudioproducoes.minharota.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Fintech Premium (Identidade v2.0.0)
val VerdeNeon = Color(0xFF6EE7B7) // Verde Esmeralda Neon (Alto Contraste)
val VerdeMenta = Color(0xFF34D399) // Verde Menta Neon
val VerdeEntrada = Color(0xFF10B981) // Green Emerald
val RoxoPrimary = Color(0xFF820AD1) // Purple (mantido)

// Cores para Efeitos de Seleção e Glassmorphism
val GlassBorder = Color.White.copy(alpha = 0.15f) // Reflexo de vidro fino

// Paleta Dark
val FundoDark = Color(0xFF0C0C0E) // Chumbo Profundo Absoluto (Mestre)
val SurfaceDark = Color(0xFF1A1A1E) // Chumbo Acetinado

// Paleta Light
val FundoLight = Color(0xFFF4F4F6) // Cinza claro/chumbo suave
val SurfaceLight = Color(0xFFFFFFFF) // Branco puro para superfícies
